# 陸上PDCA日記アプリ 設計ドキュメント

版: 0.1（設計フェーズ） / 作成日: 2026-06-01
ベース: 要件定義書 v1.0（2026-05-20）

---

## 0. このドキュメントの目的

実装に入る前に「何を・どう作るか」を固める。学習目的のため、各設計判断には
**なぜそうするか** を併記する。

---

## 1. 全体アーキテクチャ

```
┌─────────────────────────────────────────────────────────────┐
│                        ユーザー（スマホ）                      │
│              息子＝日記を書く / 親＝見守り・リアクション         │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTPS（ブラウザ / PWA）
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   Next.js アプリ（Vercel 上）                  │
│                                                               │
│  ┌──────────────┐   ┌─────────────────────────────────────┐  │
│  │ 画面 (React)  │   │ サーバー処理 (API Routes / Server)    │  │
│  │ ・ホーム       │   │ ・日記の保存/取得                     │  │
│  │ ・入力         │   │ ・動画アップロード                     │  │
│  │ ・カレンダー   │   │ ・連続記録/集計                       │  │
│  │ ・グラフ       │   │ ・メール通知送信                      │  │
│  │ ・過去ログ     │   │ ・認証チェック（家族のみ）             │  │
│  │ ・設定         │   └───────┬─────────────┬──────────────┘  │
│  └──────────────┘           │             │                  │
└──────────────────────────────┼─────────────┼──────────────────┘
            ▲                   │             │
   Google   │                   ▼             ▼
   ログイン  │        ┌────────────────┐ ┌──────────────────┐
            │        │ Google Sheets   │ │ Google Drive      │
   ┌────────┴─────┐  │ （データベース）  │ │ （動画ファイル）   │
   │ Auth.js      │  │ ・diaries       │ │ ・mp4等を保存      │
   │ Googleログイン│  │ ・times         │ │ ・限定共有で再生    │
   │ 家族メール許可│  │ ・reactions     │ └──────────────────┘
   └──────────────┘  │ ・users         │
                     └────────────────┘
   ┌──────────────────────────┐      ┌──────────────────────┐
   │ Vercel Cron（定期実行）    │─────▶│ メール送信(Gmail SMTP) │
   │ ・毎日22:00 未入力チェック  │      │ （親・本人へメール通知）  │
   │ ・毎週日曜 週次サマリー     │      └──────────────────────┘
   └──────────────────────────┘
```

**なぜこの構成か:**
- Next.js は「画面」と「サーバー処理」を1つのプロジェクトで書ける。学習効率が良く、
  APIキーなど秘密情報はサーバー側に隠せる（ブラウザに出さない＝安全）。
- データと動画を Google に置くことで完全無料・Google連携の要件を満たす。
- 通知・集計は時間で動くので、Vercel Cron（決まった時刻にサーバー処理を呼ぶ仕組み）で実現。

---

## 2. データ設計（Google スプレッドシート）

1つのスプレッドシートの中に、用途別の「シート（タブ）」を分けて持つ。
**= リレーショナルDBのテーブルに相当。**

### 2.1 `users` シート（利用者）
| 列 | 内容 | 例 |
|---|---|---|
| id | ユーザーID | u_son / u_parent |
| email | Googleログインのメール | son@gmail.com |
| role | 役割 | son / parent |
| (通知先メールは設定/環境変数で管理) | 親・本人の通知先 | parent@example.com |
| created_at | 登録日時 | 2026-06-01T10:00:00Z |

### 2.2 `diaries` シート（日記＝PDCA記録）★中心テーブル
| 列 | 内容 | 必須 |
|---|---|---|
| id | 日記ID（例: d_20260601） | ○ |
| date | 対象日（YYYY-MM-DD） | ○ |
| plan | P：今日の課題・テーマ | 任意 |
| do | D：練習内容・本数・コンディション | 任意 |
| check | C：できた点・できなかった点・気づき | 任意 |
| action | A：明日への修正・継続方針 | 任意 |
| video_file_id | 紐づく動画のDrive ID（無→空） | 任意 |
| created_at | 入力日時（タイムスタンプ自動） | ○ |
| input_seconds | 入力所要時間（秒） | ○ |

※「全項目必須ではない」要件に合わせ、PDCA各項目は任意入力。

### 2.3 `times` シート（タイム記録）
| 列 | 内容 |
|---|---|
| id | タイムID |
| date | 計測日 |
| event | 種目（100m / 200m） |
| seconds | タイム（秒。例 13.45） |
| diary_id | 紐づく日記ID（相関分析用・6.3） |
| created_at | 記録日時 |

### 2.4 `reactions` シート（親のリアクション・6.4）
| 列 | 内容 |
|---|---|
| id | リアクションID |
| diary_id | 対象の日記ID |
| from_user_id | 送った人（親） |
| stamp | スタンプ（👍🔥💪 等） |
| comment | 一言コメント |
| created_at | 日時 |

### 2.5 `settings` シート（設定）
| 列 | 内容 |
|---|---|
| key | 設定キー（reminder_time 等） |
| value | 値 |

**なぜシートを分けるか:** 1シートに全部入れると後で集計・検索が破綻する。
「日記」「タイム」「リアクション」を分け、`diary_id` で繋ぐ（＝リレーション）。
これは本物のDB設計の考え方そのもので、学習価値が高い。

---

## 3. 画面構成（要件定義書 5章 準拠）

| 画面 | 内容 | 対応する要件・追加機能 |
|---|---|---|
| ホーム | 今日の入力状況 / **連続記録日数(6.1)** / 直近の振り返り | Must |
| 入力 | PDCA4ステップのフォーム + タイム + **動画アップロード** | Must + 動画 |
| カレンダー | 月別入力履歴を色分け / 連続日数可視化 | Must |
| グラフ | 100m・200mのタイム推移 / **日記との相関(6.3)** | Should + 6.3 |
| 過去ログ | 一覧 / キーワード・日付検索 / 詳細（動画再生・親コメント表示） | Should + 動画 + 6.4 |
| 設定 | 通知先(メール)設定 / 家族メンバー管理 | Must |

### 入力画面のUX方針（非機能要件：3〜5分で完了）
- PDCAを縦1画面で順に入力 → 余計なボタンを排除
- 各項目は任意・プレースホルダーに例文を表示して書きやすく
- 「保存」を押した瞬間に created_at と input_seconds を自動記録
- 保存と同時に親へメール「今日の日記を書きました」（入力完了通知）

---

## 4. 主要な処理フロー

### 4.1 日記を書く（息子）
```
入力画面で記入 → [保存] 押下
  → サーバー：認証チェック（家族か？）
  → diaries シートに行を追加（タイム入力あれば times にも追加）
  → 動画があれば Drive にアップロードし file_id を diaries に保存
  → 親の line_user_id へ「今日の日記を書きました」を送信
  → ホームへ。連続記録日数を更新表示
```

### 4.2 毎日22時の未入力チェック（Vercel Cron）
```
22:00 に Cron が専用APIを呼ぶ
  → 今日の日付の diaries 行があるか確認
  → 無ければ 本人＋親 へ「今日の日記がまだです」をメール送信
```

### 4.3 週次サマリー（毎週日曜・Vercel Cron / 6.2）
```
日曜に Cron が専用APIを呼ぶ
  → 直近7日の diaries / times を集計
  → 「記録日数・連続日数・タイム変化・今週のCheck/Action抜粋」を生成
  → 親＋本人 へメール送信
```

### 4.4 連続記録カウント（6.1）
```
diaries の date を新しい順に見て、今日から連続して
入力がある日数をカウント → ホームに「🔥 12日連続」と表示
```

---

## 5. 認証・プライバシー（非機能要件）

- **Googleログイン**（Auth.js）。ログイン後、メールが `users` シートの
  許可リストにあるか確認 → 無ければアクセス拒否。＝本人・家族のみ。
- 動画は Drive の「限定公開（リンクを知っている人のみ／特定アカウント）」で保存し、
  アプリ内に埋め込み再生。第三者は閲覧不可。
- APIキー・トークン類はすべてサーバー側の環境変数に保管（ブラウザに出さない）。

---

## 6. 開発の進め方（要件定義書 7章のロードマップに沿う）

学習目的のため、小さく動かして確認しながら段階的に進める。

| Step | 内容 | 学べること | 状態 |
|---|---|---|---|
| Step 1 | Next.jsプロジェクト作成・ホーム/入力画面の見た目 + ローカル動作 | Next.js / React / Tailwind | ✅ 完了 |
| Step 2 | Googleログイン（家族限定） | OAuth / 認証 | ✅ 完了 |
| Step 3 | スプレッドシート連携（日記の保存・取得） | API連携 / Sheets API | ✅ 完了 |
| Step 4 | 入力完了→メール通知 / 22時未入力リマインド（Cron） | 外部API / 定期実行 | ✅ 完了 |
| Step 5 | カレンダー・タイムグラフ・週次サマリー | 集計 / グラフ描画 | ✅ 完了 |
| Step 6 | 動画アップロード・再生（Drive） | ファイル/Drive API | ✅ 完了 |
| Step 7 | 親リアクション・検索・相関表示（6.3/6.4 仕上げ） | 仕上げ・改善 | ✅ 完了 |

---

## 7. 事前に用意が必要なもの（後のステップで案内）

- Google アカウント（既存でOK）
- Google Cloud プロジェクト（Sheets API / Drive API / OAuth を有効化）
- Gmailアカウント（通知メール送信用・アプリパスワードを発行）
- Vercel アカウント（GitHub連携でデプロイ）

※ これらは該当ステップで一緒に設定手順を案内します。今は不要。
```
