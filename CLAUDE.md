@AGENTS.md

## 開発ルール

### コスト管理
- モデルは常に claude-sonnet-4-6 を使用すること
- セッションは設計のStepごとに必ず区切ること
  （長いセッションは履歴の再読み込みでコストが膨らむため）
- ファイルの全文書き直しは避け、変更箇所のみ編集すること
- ビルド・lint・テストはまとめて1回で実行すること

### Stepの進め方
- 1セッション = 1Step を原則とする
- Stepの最後に typecheck・lint・build が全部通ってから終了すること
- Step完了時に DESIGN.md のステータスを必ず更新すること
- 次のセッション開始時は「DESIGN.md と CLAUDE.md を読んでください。前回までに Step X が完了しています。今回は Step Y を進めます。」と伝えること

### セッションをまたぐ場合
- 過去Stepの修正が必要な場合は DESIGN.md のステータスを確認してから着手すること
- 修正の影響範囲（どのStepに波及するか）を最初に確認すること

### コーディング方針
- 変更理由（なぜそうするか）をコメントで残すこと
- 型チェック・lintエラーは抑制せず、正しい書き方で解消すること
- 秘密情報は必ず環境変数（.env.local）に書き、コードに直書きしないこと